def print_square_of_stars(rows):
    for i in range(rows):
        for j in range(rows):
            print('*', end='')
        print()

def main():
    rows = int(input("Enter number of rows: "))
    print_square_of_stars(rows)

if __name__ == "__main__":
    main()
    
